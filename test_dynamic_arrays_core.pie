// Test PIE file for arrays - Dynamic arrays only (no printing)

// Dynamic arrays
string names[] = ["Alice", "Bob", "Charlie"];
int numbers[] = [1, 2, 3];

char grades[]=['A','B','C'];

output(arr_indexof(grades,'R'),int);
int search_value=3;
if(arr_contains(numbers,search_value)){
    output("Numbers contains the value 3",string);
}else{
    output(numbers,array);
}

// arr_push
arr_push(names, "David");
arr_push(numbers, 4);

// arr_size
output(arr_size(names), int); // Expected: 4
output(arr_size(numbers), int); // Expected: 4

// Array concatenation
int arr1[] = [10, 20];
int arr2[] = [30, 40];
int arr3[] = arr1 + arr2;
output(arr_size(arr3), int); // Expected: 4
output(arr3[0], int); // Expected: 10
output(arr3[3], int); // Expected: 40

// arr_avg
int avg_nums[] = [10, 20, 37];
output(arr_avg(avg_nums), float, 3); // Expected: 20.0

output(names,array);
