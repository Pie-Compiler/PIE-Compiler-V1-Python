int main() {
  int numbers[5];
  int i;
  i = 0;
  while (i < 5) {
    numbers[i] = i * 2;
    i = i + 1;
  }

  output(numbers[3], int);

  string names[3];
  names[0] = "Alice";
  names[1] = "Bob";
  names[2] = "Charlie";

  output(names[1], string);

  return 0;
}
