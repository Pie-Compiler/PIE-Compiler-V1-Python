int x = 5;
float y = 10.5;
string msg = "Hello";

// Type error: assigning string to int
x = msg;

// Type error: incompatible binary operation
int z = x + msg;

// Scope error: undefined variable
int result = unknown_var * 2;

// Type error: non-boolean condition
if (x) {
    output("x is true", string);
}

// Correct usage
if (x > 3) {
    output("x is greater than 3", string);
}

// Type error: incompatible operands
if (msg > x) {
    output("This shouldn't compile", string);
}